name: "Auto Manage Issue Blockers"

on:
  workflow_dispatch:

permissions:
  issues: write
  
jobs:
  call-issue-blocker:
    uses: Budhaprasad/Budda1/.github/workflows/issue-blocker-reusable.yml@main
